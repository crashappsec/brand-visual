# Decks and slides

Deck templates land in Phase 5 (a `decks/` directory). Until then, apply
`visual-rules.md` and the token references to slide artifacts: dark Black
surfaces, Atomic lime as the primary accent, JetBrains Mono titles, Inter body,
sharp 4px corners, and the exact palette and spacing values from the `tokens-*`
references.
